# 短線雙分析師（追熱門股 / 穩重型）權重升級指引 v2

> 給 Cline 的實作依據文件。目標：在現有 `core/scorer.py` + `core/scoring_config.py` 架構上，
> 新增「追熱門股」「穩重型」兩種短線分析師人格，各自有獨立的買/賣權重，
> **且只使用 FinMind 免費版可取得或可運算出的資料**。
>
> v2 變更：將「密集成交區(POC)」從「移除」改為「用日K資料換一種算法保留」，
> 因為對短線（持有數天）策略而言，多日籌碼密集區比單日盤中 POC 更貼合策略週期，詳見第 3 節。

---

## 0. 背景

原始提案有 10 個子項。「主力分點/倒貨」需要 Sponsor 等級的分點資料，免費版拿不到，
無法運算，**予以移除**。「轉勢確立(60分K)」的核心價值是「反應速度」，但本系統是收盤後
批次計算、隔天才決策，盤中反應速度優勢發揮不出來，且日K雜訊更少、更適合短線（數天）
週期，**改用日K均線斜率判斷，併入「趨勢結構」子項**。「密集成交區(POC)」原定義依賴
盤中逐筆/分K資料算不出來，但**換成「多日籌碼密集區」概念後，用日K收盤價+成交量就能算，
且更貼合短線策略的持有週期，因此保留為獨立子項**。

最終保留 **8 個子項**，全部可用 FinMind 免費版資料取得或運算。

---

## 1. 保留 / 移除項目對照

| 子項 | 決定 | 資料需求 | FinMind 資料集 | 等級 |
|---|---|---|---|---|
| 趨勢結構/破壞（含轉勢確立精神） | ✅ 保留，並吸收「轉勢確立」判斷邏輯 | 日K均線 | TaiwanStockPrice | Free |
| 動能強度/減速 | ✅ 保留 | 日K RSI/MACD | TaiwanStockPrice | Free |
| 成交量結構/異常 | ✅ 保留 | 日K成交量 | TaiwanStockPrice | Free |
| 慣性突破/破壞 | ✅ 保留（改用日K近N日高低點） | 日K高低價 | TaiwanStockPrice | Free |
| 波動風險/過熱 | ✅ 保留 | 日K乖離率/RSI | TaiwanStockPrice | Free |
| 三大法人進出 | ✅ 保留 | 三大法人買賣超 | TaiwanStockInstitutionalInvestorsBuySell | Free（帶 data_id） |
| 散戶籌碼健康 | ✅ 保留 | 融資融券/借券 | TaiwanStockMarginPurchaseShortSale、TaiwanStockSecuritiesLending | Free（帶 data_id） |
| **籌碼密集區（多日版 POC 代理）** | ✅ **v2 改為保留**，用多日量價分布取代單日盤中 POC | 日K收盤價+成交量，N日histogram | TaiwanStockPrice | Free |
| 主力分點/倒貨 | ❌ 移除 | 券商分點進出 | TaiwanStockTradingDailyReport | Sponsor（免費版無法取得） |
| 轉勢確立(60分K)（獨立子項） | ❌ 移除，邏輯併入趨勢結構 | 盤中分K | TaiwanStockKBar | Sponsor（免費版無法取得） |

---

## 2. 新版權重表（8 子項，總和 100%）

### 追熱門股分析師

| 子項 | 買入 | 賣出 | 說明 |
|---|---|---|---|
| 動能強度/減速 | 30% | 10% | 主軸不變：買看爆發、賣看是否真的失速 |
| 慣性突破/破壞 | 25% | 35% | 短線核心，賣出時權重最高（慣性反轉＝出場訊號） |
| 成交量結構/異常 | 15% | 15% | 攻擊量 vs 爆量出貨 |
| 趨勢結構/破壞 | 15% | 20% | 已吸收轉勢確立判斷，賣出更重視均線失守 |
| 波動風險/過熱 | 5% | 10% | 賣出時放大過熱權重，作停利觸發 |
| 籌碼密集區 | 5% | 5% | 追熱門股仍只當短線參考，不作主要依據 |
| 三大法人進出 | 5% | 5% | 移除主力分點後，補回一點大額籌碼監控 |
| 散戶籌碼健康 | 0% | 0% | 追熱門股不看散戶（設計不變） |
| **合計** | **100%** | **100%** | |

### 穩重型分析師

| 子項 | 買入 | 賣出 | 說明 |
|---|---|---|---|
| 趨勢結構/破壞 | 37% | 37% | 吸收轉勢確立，成為穩重型的進出場核心 |
| 三大法人進出 | 18% | 23% | 移除主力分點後，成為穩重型最主要的籌碼依據 |
| 籌碼密集區 | 15% | 15% | 取代原本 POC 角色，作支撐/壓力參考 |
| 成交量結構/異常 | 10% | 10% | 常態監控 |
| 慣性突破/破壞 | 5% | 10% | 賣出時提高，防範慣性轉空 |
| 動能強度/減速 | 5% | 5% | 不做短線動能決策 |
| 散戶籌碼健康 | 5% | 0% | 買入評估籌碼沉澱，賣出不看（維持原設計） |
| 波動風險/過熱 | 5% | 0% | 穩重型不因過熱賣出（維持原設計） |
| **合計** | **100%** | **100%** | |

---

## 3. 新增子項定義

### 3.1 慣性突破/破壞（`score_inertia_break`）

- **多頭慣性突破**：`close > 前 10 日最高價`
- **多頭慣性延續**：`收盤價連續 N 日（≥3日）站上前一日收盤`
- **空頭慣性破壞**：`close < 前 10 日最低價`
- **空頭慣性延續**：`收盤價連續 N 日（≥3日）跌破前一日收盤`

| 分數 | 條件 |
|---|---|
| 100 | 突破前10日高點 且 連漲 ≥3日 |
| 85 | 突破前10日高點 或 連漲 ≥3日 |
| 70 | 收盤價高於5日均線但無明顯突破 |
| 50 | 盤整 |
| 0 | 跌破前10日低點 或 連跌 ≥3日 |

### 3.2 籌碼密集區（`score_chip_concentration`）—— POC 的多日日K代理版

**核心概念**：單日盤中 POC 算的是「今天量最密集在哪個價位」，本質是給當沖用的；
短線（持有數天）策略真正需要的是「過去一段時間，多方籌碼主要套在哪個價位帶」，
這個可以用**多日收盤價 × 成交量的加權分布**估計，完全用日K資料算得出來，
且比單日 POC 更貼合策略持有週期。

**運算方式**：
1. 取近 N 日（建議 N=60，約一季）的 `close` 與 `Trading_Volume`
2. 依收盤價將區間分箱（bin），例如以 (最高價-最低價)/30 為一個 bin 寬度
3. 每個 bin 累加落在該價格區間的成交量
4. 找出成交量最大的 bin，其中心價位即為 `Chip_Concentration_Price (CCP)`
5. 計算 `Distance = (close - CCP) / CCP * 100`（%）

**評分（單一分數，買賣共用，僅權重不同，與現有子項函式風格一致）**：

| 分數 | 條件（Distance） |
|---|---|
| 100 | > 5%（站穩在密集區上方，籌碼已消化） |
| 85 | 1% ~ 5%（剛站上密集區） |
| 70 | -2% ~ 1%（正在測試密集區） |
| 50 | -5% ~ -2%（跌破密集區，測試支撐） |
| 0 | < -5%（深跌破密集區，密集區轉壓力） |

---

## 4. `core/scoring_config.py` 新增內容

```python
# ============================================================
# 雙分析師短線權重（v3.0 新增，僅使用 FinMind 免費版可得資料）
# ============================================================

# 追熱門股分析師
CHASER_BUY_WEIGHTS = {
    "momentum": 0.30,
    "inertia_break": 0.25,
    "volume": 0.15,
    "trend_structure": 0.15,
    "risk": 0.05,
    "chip_concentration": 0.05,
    "institutional": 0.05,
    "chip": 0.00,
}
CHASER_SELL_WEIGHTS = {
    "momentum": 0.10,
    "inertia_break": 0.35,
    "volume": 0.15,
    "trend_structure": 0.20,
    "risk": 0.10,
    "chip_concentration": 0.05,
    "institutional": 0.05,
    "chip": 0.00,
}

# 穩重型分析師
STABLE_BUY_WEIGHTS = {
    "trend_structure": 0.37,
    "institutional": 0.18,
    "chip_concentration": 0.15,
    "volume": 0.10,
    "inertia_break": 0.05,
    "momentum": 0.05,
    "chip": 0.05,
    "risk": 0.05,
}
STABLE_SELL_WEIGHTS = {
    "trend_structure": 0.37,
    "institutional": 0.23,
    "chip_concentration": 0.15,
    "volume": 0.10,
    "inertia_break": 0.10,
    "momentum": 0.05,
    "chip": 0.00,
    "risk": 0.00,
}

STYLE_PROFILES = {
    "chaser": {"buy": CHASER_BUY_WEIGHTS, "sell": CHASER_SELL_WEIGHTS},
    "stable": {"buy": STABLE_BUY_WEIGHTS, "sell": STABLE_SELL_WEIGHTS},
}

# 慣性突破/破壞門檻
INERTIA_THRESHOLDS = {
    "high_n_days": 10,
    "low_n_days": 10,
    "consec_days_required": 3,
}

# 籌碼密集區（多日 POC 代理）參數
CHIP_CONCENTRATION_THRESHOLDS = {
    "lookback_days": 60,      # 統計區間
    "num_bins": 30,           # 價格分箱數
    "dist_excellent": 5.0,    # 距離密集區 % 門檻
    "dist_good": 1.0,
    "dist_normal": -2.0,
    "dist_weak": -5.0,
}
```

> ⚠️ 保留現有 `SHORT_TERM_WEIGHTS` / `SHORT_TERM_BUY_WEIGHTS` / `SHORT_TERM_SELL_WEIGHTS`
> 不動，新增的是平行的一組設定，不影響既有回測（`get_historical_scores`）的行為。

---

## 5. `core/scorer.py` 新增內容

### 5.1 慣性突破/破壞

```python
def score_inertia_break(row, high_n=10, low_n=10, consec_required=3) -> dict:
    """慣性突破/破壞評分（v3.0 新增）"""
    close = row.get("close", None)
    high_nd = row.get(f"High_{high_n}D", None)   # 需由 processor 提供
    low_nd = row.get(f"Low_{low_n}D", None)
    consec_up = row.get("Consec_Up_Days", 0)      # 需由 processor 提供
    consec_down = row.get("Consec_Down_Days", 0)
    ma5 = row.get("MA_5", None)

    if pd.isna(close):
        return {"score": 0, "details": {"note": "close 資料不足"}}

    break_high = pd.notna(high_nd) and close > high_nd
    break_low = pd.notna(low_nd) and close < low_nd
    strong_up = consec_up is not None and consec_up >= consec_required
    strong_down = consec_down is not None and consec_down >= consec_required
    above_ma5 = pd.notna(ma5) and close > ma5

    if break_low or strong_down:
        score = 0
    elif break_high and strong_up:
        score = 100
    elif break_high or strong_up:
        score = 85
    elif above_ma5:
        score = 70
    else:
        score = 50

    return {
        "score": score,
        "details": {
            "break_high": break_high,
            "break_low": break_low,
            "consec_up_days": consec_up,
            "consec_down_days": consec_down,
            "note": "v3.0: 日K近N日高低點 + 連續漲跌天數判斷（免分點/分K版）",
        }
    }
```

### 5.2 籌碼密集區（多日 POC 代理）

```python
def score_chip_concentration(row, price_history: pd.DataFrame = None) -> dict:
    """
    籌碼密集區評分（v3.0 新增，多日日K代理版 POC）

    price_history: 需含近 lookback_days 筆的 ['close', 'Trading_Volume']，
                    由呼叫端（如 get_historical_scores 的 window）傳入。
                    若未提供，回傳中性分數並註記資料不足。
    """
    from core.scoring_config import CHIP_CONCENTRATION_THRESHOLDS as t

    close = row.get("close", None)
    if pd.isna(close) or price_history is None or len(price_history) < 10:
        return {"score": 50, "details": {"note": "資料不足，回傳中性分數"}}

    hist = price_history.tail(t["lookback_days"]).dropna(subset=["close", "Trading_Volume"])
    if hist.empty:
        return {"score": 50, "details": {"note": "資料不足，回傳中性分數"}}

    price_min, price_max = hist["close"].min(), hist["close"].max()
    if price_max <= price_min:
        return {"score": 50, "details": {"note": "價格區間過窄，無法分箱"}}

    bins = np.linspace(price_min, price_max, t["num_bins"] + 1)
    hist["_bin"] = pd.cut(hist["close"], bins=bins, include_lowest=True)
    vol_by_bin = hist.groupby("_bin", observed=True)["Trading_Volume"].sum()

    if vol_by_bin.empty:
        return {"score": 50, "details": {"note": "分箱後無資料"}}

    top_bin = vol_by_bin.idxmax()
    ccp = (top_bin.left + top_bin.right) / 2  # 密集區中心價

    if ccp <= 0:
        return {"score": 50, "details": {"note": "密集區價位異常"}}

    distance = (close - ccp) / ccp * 100

    if distance >= t["dist_excellent"]:
        score = 100
    elif distance >= t["dist_good"]:
        score = 85
    elif distance >= t["dist_normal"]:
        score = 70
    elif distance >= t["dist_weak"]:
        score = 50
    else:
        score = 0

    return {
        "score": score,
        "details": {
            "chip_concentration_price": round(ccp, 2),
            "distance_pct": round(distance, 2),
            "lookback_days": t["lookback_days"],
            "note": "v3.0: 多日收盤價x成交量加權分布，取代單日盤中POC",
        }
    }
```

### 5.3 雙分析師總分函式（平行於既有 `score_short_term`，不取代）

```python
def score_short_term_by_profile(row, profile: str = "chaser", price_history: pd.DataFrame = None) -> dict:
    """
    依分析師人格計算短線總分（v3.0 新增）
    profile: "chaser"（追熱門股） 或 "stable"（穩重型）
    price_history: 傳給 score_chip_concentration 用的近期價量資料
    """
    from core.scoring_config import STYLE_PROFILES

    if profile not in STYLE_PROFILES:
        raise ValueError(f"未知的分析師人格: {profile}")

    ma_alignment = row.get("MA_Alignment", 0)
    rsi_limit = 95 if pd.notna(ma_alignment) and ma_alignment >= 3 else 88

    trend = score_trend_structure(row)
    momentum = score_momentum(row, rsi_limit)
    volume = score_volume_structure(row)
    inst = score_institutional(row)
    chip = score_chip_health(row)
    risk = score_volatility_risk(row, rsi_limit)
    inertia = score_inertia_break(row)
    concentration = score_chip_concentration(row, price_history)

    sub_scores = {
        "trend_structure": trend["score"],
        "momentum": momentum["score"],
        "volume": volume["score"],
        "institutional": inst["score"],
        "chip": chip["score"],
        "risk": risk["score"],
        "inertia_break": inertia["score"],
        "chip_concentration": concentration["score"],
    }

    buy_w = STYLE_PROFILES[profile]["buy"]
    sell_w = STYLE_PROFILES[profile]["sell"]

    buy_total = sum(sub_scores[k] * buy_w[k] for k in sub_scores)
    sell_total = sum(sub_scores[k] * sell_w[k] for k in sub_scores)

    return {
        "profile": profile,
        "total_buy": int(round(buy_total)),
        "total_sell": int(round(sell_total)),
        "breakdown": sub_scores,
        "details": {
            "trend_structure": trend["details"],
            "momentum": momentum["details"],
            "volume": volume["details"],
            "institutional": inst["details"],
            "chip": chip["details"],
            "risk": risk["details"],
            "inertia_break": inertia["details"],
            "chip_concentration": concentration["details"],
        }
    }
```

---

## 6. 前置作業：processor 需新增的欄位

- `High_10D`、`Low_10D`：近 10 日最高/最低價（`.rolling(10).max()/.min()`）
- `Consec_Up_Days`、`Consec_Down_Days`：連續收紅/收黑天數（`close.diff()` 正負號分組計算）

以上皆可由 `TaiwanStockPrice`（Free）算出，不需額外呼叫新的 FinMind API。

`score_chip_concentration` 額外需要呼叫端傳入近 60 日的 `price_history`（`close` +
`Trading_Volume` 兩欄），在 `get_historical_scores` 的 walk-forward 迴圈中，直接可用
`window.tail(60)` 取得，不需新的資料來源。

---

## 7. Cline 實作步驟清單

1. 在 `core/scoring_config.py` 尾端加入第 4 節的 `CHASER_*`、`STABLE_*`、`STYLE_PROFILES`、
   `INERTIA_THRESHOLDS`、`CHIP_CONCENTRATION_THRESHOLDS`。
2. 在 `core/scorer.py` 加入 `score_inertia_break()`、`score_chip_concentration()`、
   `score_short_term_by_profile()`（第 5 節）。
3. 在 data processor 補上 `High_10D`、`Low_10D`、`Consec_Up_Days`、`Consec_Down_Days` 四個欄位。
4. **不要修改**既有 `score_short_term()`、`SHORT_TERM_BUY_WEIGHTS`、`SHORT_TERM_SELL_WEIGHTS`，
   確保既有回測（`get_historical_scores`）與既有呼叫方不受影響。
5. 若要把雙分析師接進 `get_historical_scores` 的 walk-forward 迴圈，記得把 `window`
   （近期價量 DataFrame）傳給 `score_short_term_by_profile(row_dict, profile, price_history=window)`。
6. 前端/API 若需要讓使用者選擇分析師人格，新增參數（例如 `analyst_profile: "chaser" | "stable"`）。
7. 撰寫單元測試：
   - 兩個 profile 的 `buy`/`sell` 權重加總皆為 1.0（`sum(CHASER_BUY_WEIGHTS.values()) == 1.0` 等斷言）
   - `total_buy` / `total_sell` 落在 [0, 100]
   - `score_inertia_break` 對「創新高+連漲」「跌破新低+連跌」回傳 100 / 0
   - `score_chip_concentration` 對「明顯站上密集區」「明顯跌破密集區」回傳 100 / 0；
     資料不足時回傳中性分數 50（不應為 0，避免誤判為極端空頭）

---

## 8. 已知取捨與待辦

- 「慣性突破/破壞」用日K近10日高低點，訊號強度弱於原本設想的分點/60K版本，屬免費資料下的
  必要妥協；未來升級 Sponsor 可用 `TaiwanStockKBar` 強化。
- 「籌碼密集區」改用多日收盤價分布，**理論上比原始單日 POC 更貼合短線（數天）策略週期**，
  不算妥協，但仍建議正式上線前拿歷史資料回測驗證，尤其是 `lookback_days=60` 這個參數是否
  最適合，可用回測結果微調（例如試 40 日 / 90 日比較）。
- 穩重型分析師把「轉勢確立」精神轉嫁到「趨勢結構」，進場反應速度會比原本用 60 分K慢，
  等於整個分析師人格變得更保守，建議實測回測績效後再決定是否要進一步調整權重。
- 若日後升級付費版，建議保留 `STYLE_PROFILES` 這個字典結構，屆時只需新增
  `PAID_CHASER_BUY_WEIGHTS` 等平行設定，不需重寫核心邏輯。
