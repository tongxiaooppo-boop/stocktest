# 台股AI個人化決策系統 v3.1 - 環境變數範例
# 
# 注意：FinMind Token 與 DeepSeek API Key 由使用者在網頁輸入，
# 不從 .env 檔案讀取，此檔案僅供參考。
#
# 若要在 Render 部署，需在 Render 後台設定以下環境變數：
# - FINMIND_TOKEN
# - DEEPSEEK_API_KEY
#
# 但本系統設計為由使用者自行在網頁輸入金鑰，
# 因此 Render 環境變數為選填（僅供訪客模式使用）。

# FinMind API Token（選填，供訪客模式使用）
# FINMIND_TOKEN=your_finmind_token_here

# DeepSeek API Key（選填，供訪客模式使用）
# DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Render 部署時自動指派，不需手動設定
# PORT=8501
