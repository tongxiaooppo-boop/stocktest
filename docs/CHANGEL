# 改版歷程

## v3.1（2026-07-20）

### catalyst 子項修正 — 與 revenue_momentum 解耦

#### 修正

| # | 項目 | 說明 |
|:---|:---|:---|
| 🐛 1 | **catalyst 實作「近12個月創新高」判斷** | 原 docstring 承諾此功能但從未實作，現於 `stock/metrics.py` 新增 `Revenue_12M_High` / `Revenue_6M_High` 布林欄位（rolling max 比較），`score_catalyst()` 改以「離散突破事件」為核心判斷 |
| 🐛 2 | **catalyst 與 revenue_momentum 重複計分解耦** | 舊版 catalyst 讀取 `Revenue_Momentum`（來自 `Revenue_Accelerating` 連3期 AND），與 `score_revenue_momentum()` 共用同一份營收趨勢訊號，造成買入端「營收動能」實質權重被放大到 40%。新版改讀 `Revenue_12M_High` / `Revenue_6M_High`（創新高離散事件），兩個子項不再重疊 |

#### 新增

| # | 項目 | 說明 |
|:---|:---|:---|
| 🆕 1 | `_compute_revenue_high()` 輔助函式 | `stock/metrics.py` 新增，用 rolling max 判斷月營收是否創近 N 個月新高，`min_periods=1` 確保新股/資料不足時不會整批判 False |

#### 權重表

- `SWING_BUY_WEIGHTS` / `SWING_SELL_WEIGHTS` **不變**
- `score_catalyst()` 內部評分邏輯改變，但權重維持買 15% / 賣 10%

---

## v3.0（2026-07-20）

### 短線雙分析師系統 — 追熱門股 / 穩重型

#### 新功能

| # | 項目 | 說明 |
|:---|:---|:---|
| 🆕 1 | **雙分析師人格** | 新增「追熱門股」（chaser）與「穩重型」（stable）兩種短線分析師，各有獨立的買/賣 8 子項權重表 |
| 🆕 2 | **慣性突破/破壞子項** | 以日K近 10 日高低點 + 連續漲跌天數判斷，取代需要付費版的分點/分K邏輯 |
| 🆕 3 | **籌碼密集區子項** | 多日收盤價 × 成交量加權分布估算 CCP，取代需要盤中逐筆資料的單日 POC |
| 🆕 4 | **共用分箱模組** | `stock/chip_concentration.py` 獨立計算籌碼密集區距離，確保 metrics 預算與 scorer fallback 使用同一份邏輯 |
| 🆕 5 | **前端分析師選擇** | 側邊欄新增 `st.radio` 切換追熱門股/穩重型，不增加按鈕，預設追熱門股 |

#### 架構

- 所有新增子項的資料源皆來自既有 `TaiwanStockPrice`（Free），**零新增 FinMind API 呼叫**
- 新版 `score_short_term_by_profile()` 平行於既有 `score_short_term()`，**完全不動舊版**
- 既有 `SHORT_TERM_BUY_WEIGHTS` / `SHORT_TERM_SELL_WEIGHTS` 保留不動
- `STYLE_PROFILES` 字典結構預留付費版擴充空間

#### 子項對照

| 子項 | 追熱門股買/賣 | 穩重型買/賣 | 說明 |
|---|---|---|---|
| 動能強度/減速 | 30% / 10% | 5% / 5% | 追熱門股核心 |
| 慣性突破/破壞 | 25% / 35% | 5% / 10% | 短線突破判斷 |
| 成交量結構 | 15% / 15% | 10% / 10% | 攻擊量 vs 出貨 |
| 趨勢結構/破壞 | 15% / 20% | 37% / 37% | 穩重型核心 |
| 波動風險/過熱 | 5% / 10% | 5% / 0% | 賣出過熱監控 |
| 籌碼密集區 | 5% / 5% | 15% / 15% | 穩重型支撐壓力 |
| 三大法人進出 | 5% / 5% | 18% / 23% | 穩重型籌碼依據 |
| 散戶籌碼健康 | 0% / 0% | 5% / 0% | 穩重型買入參考 |

---

## v2.2（2026-07-16）

### Phase 3：賣出評分雙軌制 + 回測強化 + UI 優化

#### 新功能

| # | 項目 | 說明 |
|:---|:---|:---|
| 🆕 1 | **短線/波段賣出評分雙軌制** | 短線/波段獨立賣出評分（total_sell），買入/賣出分數分離 |
| 🆕 2 | **綜合策略賣出評分支援** | `_parse_composite_trades()` 新增 `sell_style_scores` 參數，綜合賣出也看賣出評分 |
| 🆕 3 | **回測四維度分數走勢圖拆分** | 1 張 4 線 → 3 張獨立圖（短線/波段/價值+定存），每張含買入門檻線 |
| 🆕 4 | **回測CSV拓展為4選項** | 積極/保守 × 買入評分/賣出評分，共4份CSV下載 |
| 🆕 5 | **雷達圖「查看細項」** | 短線買/賣、波段買/賣雷達圖下方新增展開式評分明細 |
| 🆕 6 | **盤後版標示** | 標題加入「🕐 盤後版」，顯示四項資料更新時間 |
| 🆕 7 | **各風格績效分析AI解說** | 回測AI解說新增5種風格獨立區塊，各風格間以分隔線區隔 |
| 🆕 8 | **中長線面圖表疊加股價** | 波段動能/價值成長/定存安全所有圖表加入股價參考線 |

#### 增強

| # | 項目 | 說明 |
|:---|:---|:---|
| 🔧 1 | 賣出評分線顏色區隔 | 短線賣出→暗紅、波段賣出→巧克力色，與買入線區別 |
| 🔧 2 | 雙彈夾UI改進 | 「下跌加碼門檻」改為「加碼門檻」，說明涵蓋下跌與突破模式 |
| 🔧 3 | 策略績效總覽標籤 | 自動顯示「（賣出策略）」「（雙彈夾）」或組合 |
| 🔧 4 | 雙彈夾門檻文字 | 根據模式動態顯示對應的說明文字 |

#### 清理

| # | 項目 |
|:---|:---|
| 🧹 1 | 移除 `bug/` 目錄除錯用 .py 與 .csv |

---

## v2.1（2026-07-15）

### Phase 2：核心 8 子項重構 + 系統性 Bug 修正

| # | 項目 | 說明 |
|:---|:---|:---|
| 🔧 1 | **24 子項全部升級五級評分制** | 五級映射：100/85/70/50/0，取消二元評分邏輯 |
| 🔧 2 | **unified Modifier 機制** | `apply_all_modifiers()` 統一疊加邏輯（DQ + Risk，不含 RSI 重複扣分） |
| 🔧 3 | **RSI 過熱均線護航機制** | MA_Alignment ≥ 3 → rsi_limit = 95，否則 88 |
| 🔧 4 | **OLS 斜率優先制** | Inst_Slope_20D 優先於 Inst_20D_Net 判斷法人趨勢 |
| 🔧 5 | **技術指標計算強化** | 補齊 MA20_Bias、MA_Med_Alignment、Vol_MA_20、Inst_Sync_Buy、Vol_MA_Bullish |
| 🔧 6 | **五級評分 reverse 修正** | 原文反序導致 excellent 終端條件不可達，重寫順序為嚴格→寬鬆 |
| 🐛 1 | **反向評分順序修正** | `five_level_score` 中 reverse=True 從寬鬆(_poor) 順序改為嚴格(_excellent)順序 |
| 🐛 2 | **EPS_YoY 分母防錯** | 去年同期 EPS 為負或零時不再計算成長率，回傳 `NaN` + `reason` |
| 🐛 3 | **財報公告日排序** | `build_universal_base_table` 內 `merge_asof` 前強制 `sort_values("date")` |
| 🐛 4 | **Payout_Ratio 計算防錯** | `TTM_EPS` 替換 `EPS` 作為分母 |
| 🐛 5 | **RSI 抄底 bonus 未運作** | `RISK_BONUS["rsi_oversold"]` 在 `apply_all_modifiers` 中自動生效 |
| 🐛 6 | **流動比率 Current_Ratio 拼字統一遍** | 修正在統一 profile 與程式碼中所有不一致處 |
| 🧹 1 | **清理舊評分邏輯** | `SHORT_TERM_THRESHOLDS` 清理舊版 key，`SWING_THRESHOLDS` 清理重複定義 |
| 🧹 2 | **20 日代理指標隔離設定** | 不再共用 10 日門檻，使用獨立 `inst_20d_surrogate_*` |
| 🧹 3 | **移除 `score_consolidation`** | 子項已不存在，相關程式碼全數移除 |
| 🧹 4 | **補齊所有缺少的 `_poor` 檔位** | 五級分制需 `_excellent` ~ `_poor` 全部補齊 |

---

## Roadmap

### 短期

| # | 項目 | 優先級 |
|:---|:---|:---:|
| 1 | 整合測試自動化（pytest） | 🔴 高 |
| 2 | Edge Case 處理（股利=0、EPS<0） | 🔴 高 |
| 3 | 評分標準調校 | 🟡 中 |
| 4 | AI Prompt 優化 | 🟡 中 |
| 5 | 錯誤處理強化 | 🟡 中 |

### 中期

| # | 項目 | 優先級 |
|:---|:---|:---:|
| 6 | 多股比較功能 | 🟡 中 |
| 7 | 選股篩選器（screener） | 🟡 中 |
| 8 | 持股管理功能 | 🟢 低 |
| 9 | 通知機制 | 🟢 低 |

### 長期

| # | 項目 | 優先級 |
|:---|:---|:---:|
| 10 | 部署上 Render | 🟡 中 |
| 11 | 新聞/產業面分析 | 🟢 低 |
| 12 | 國際股市連動 | 🟢 低 |
| 13 | 使用者帳號系統 | 🟢 低 |